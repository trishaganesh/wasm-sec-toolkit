# WASM-Based Web Application Firewall (WAF)

A WebAssembly-based WAF that inspects HTTP payloads at the edge, blocks malicious requests, and allows safe traffic. 
Designed to run in Node.js, Deno, or Cloudflare Workers for safe, fast, and portable execution.
